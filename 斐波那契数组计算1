#define _CRT_SECURE_NO_WARNINGS 1



#include<malloc.h>

#include<stdio.h>

int fun(int n)
{
    if (n <= 2)

        return 1;
    else
        return fun(n - 1) + fun(n - 2);
   
}

int main()
{
    int n;
    scanf("%d", &n);
    printf("n is:%d\n", fun(n));
    return 0;
}
